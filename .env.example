# Environment variables recognized by this project.
# Copy to `.env` and uncomment to override — but note: .env is not
# auto-loaded yet; either `export` these in your shell or use
# `VAR=value python ...` per command.

# MLflow tracking backend. Default (unset): local SQLite store.
# MLFLOW_TRACKING_URI=http://127.0.0.1:5000